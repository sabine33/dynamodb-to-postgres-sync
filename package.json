{
  "name": "dynamo-to-pg-sync",
  "version": "1.0.0",
  "description": "Interactive CLI to migrate/replicate DynamoDB tables into PostgreSQL",
  "bin": {
    "dynamo-to-pg-sync": "./bin/cli.js"
  },
  "main": "src/index.js",
  "files": [
    "bin",
    "src"
  ],
  "engines": {
    "node": ">=16"
  },
  "keywords": ["dynamodb", "postgresql", "cli", "migration", "replication", "cdc"],
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "pg": "^8.11.5",
    "prompts": "^2.4.2"
  }
}